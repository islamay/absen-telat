import { StyleSheet } from 'react-native'
import VAR from './VAR'

const styles = StyleSheet.create({
    mainText: {
        textAlign: 'center'
    },

    linkText: {
        color: VAR.darkBlue
    }
})

export default styles