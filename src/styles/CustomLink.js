import { StyleSheet } from 'react-native'
import VAR from './VAR'

const styles = StyleSheet.create({
    container: {

    },

    text: {
        textAlign: 'center',
        color: VAR.darkBlue
    }
})

export default styles